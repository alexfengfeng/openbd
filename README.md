# 用户需求管理系统 MVP

基于 **Next.js 15 + Prisma + MySQL** 构建的智能需求管理 SaaS 系统，支持 **AI 辅助需求录入**功能。

## 功能特性

- 🔐 **用户认证** - JWT 认证，支持用户名密码登录
- 🏢 **多租户工作空间** - 每个用户可以创建多个独立工作空间
- 📋 **需求管理** - 创建、编辑、删除需求，支持状态和优先级管理
- 📊 **多种视图** - 列表视图和看板视图
- 🤖 **AI 智能解析** - 自然语言描述需求，AI 自动解析结构化数据
- 🏷️ **标签系统** - 支持为需求添加标签分类
- 👥 **成员管理** - 工作空间成员角色管理

## 技术栈

| 分类 | 技术 |
|------|------|
| 前端 | React 19, Next.js 15, TailwindCSS |
| 后端 | Next.js API Routes, Prisma ORM |
| 数据库 | MySQL 8.0 |
| 认证 | JWT + bcrypt |
| UI 组件 | shadcn/ui |

## 快速开始

### 1. 安装依赖

```bash
pnpm install
```

### 2. 配置数据库

确保已安装 MySQL，创建数据库：

```sql
CREATE DATABASE fast_json CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

修改 `.env.local` 中的数据库连接：

```env
DATABASE_URL="mysql://root:root@localhost:3306/fast_json"
```

### 3. 初始化数据库

```bash
# 生成 Prisma Client
pnpm db:generate

# 推送数据库 schema
pnpm db:push

# 填充种子数据
pnpm db:seed
```

### 4. 启动开发服务器

```bash
pnpm dev
```

访问 http://localhost:3000

### 5. 测试账号

种子数据创建了以下测试账号：

| 用户名 | 密码 | 角色 |
|--------|------|------|
| admin | admin123 | 管理员 |
| testuser | test123 | 普通用户 |

## 项目结构

```
fast-json/
├── prisma/
│   ├── schema.prisma          # 数据库模型定义
│   ├── seed.ts                # 种子数据
│   └── migrations/            # 数据库迁移文件
│
├── src/
│   ├── app/                   # Next.js App Router
│   │   ├── (auth)/           # 认证相关页面
│   │   ├── (dashboard)/      # 仪表板相关页面
│   │   ├── api/              # API 路由
│   │   ├── layout.tsx        # 根布局
│   │   └── page.tsx          # 首页
│   │
│   ├── components/            # React 组件
│   │   ├── ui/               # shadcn/ui 基础组件
│   │   ├── layout/           # 布局组件
│   │   ├── requirements/     # 需求相关组件
│   │   └── ai/               # AI 相关组件
│   │
│   ├── catalog/              # json-render 组件目录（预留）
│   │
│   └── lib/                  # 工具库
│       ├── prisma.ts         # Prisma 客户端
│       ├── auth.ts           # 认证工具
│       └── utils.ts          # 通用工具函数
│
├── .env.local                # 环境变量
├── next.config.js            # Next.js 配置
├── tailwind.config.ts        # Tailwind CSS 配置
└── tsconfig.json             # TypeScript 配置
```

## API 接口

### 认证相关

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | `/api/auth/login` | 用户登录 |
| POST | `/api/auth/register` | 用户注册 |
| GET | `/api/auth/me` | 获取当前用户信息 |
| POST | `/api/auth/logout` | 用户登出 |

### 工作空间相关

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | `/api/workspaces` | 获取工作空间列表 |
| POST | `/api/workspaces` | 创建工作空间 |
| GET | `/api/workspaces/[id]` | 获取工作空间详情 |
| PUT | `/api/workspaces/[id]` | 更新工作空间 |
| DELETE | `/api/workspaces/[id]` | 删除工作空间 |

### 需求相关

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | `/api/requirements` | 获取需求列表 |
| POST | `/api/requirements` | 创建需求 |
| GET | `/api/requirements/[id]` | 获取需求详情 |
| PUT | `/api/requirements/[id]` | 更新需求 |
| DELETE | `/api/requirements/[id]` | 删除需求 |
| PATCH | `/api/requirements/[id]/status` | 更新需求状态 |

### AI 相关

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | `/api/ai/parse-requirement` | AI 解析需求描述 |

### 标签相关

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | `/api/tags` | 获取标签列表 |
| POST | `/api/tags` | 创建标签 |

## 开发命令

```bash
# 开发
pnpm dev

# 构建
pnpm build

# 启动生产服务器
pnpm start

# 代码检查
pnpm lint

# 数据库相关
pnpm db:generate    # 生成 Prisma Client
pnpm db:push        # 推送 schema 到数据库
pnpm db:seed        # 填充种子数据
pnpm db:studio      # 打开 Prisma Studio
```

## 未来计划

- [ ] 集成真实的 AI 服务（OpenAI API）
- [ ] 实现需求拖拽功能
- [ ] 添加需求评论功能
- [ ] 添加文件附件支持
- [ ] 实现需求通知功能
- [ ] 添加数据导出功能
- [ ] 集成 json-render 实现自定义仪表板

## License

MIT
